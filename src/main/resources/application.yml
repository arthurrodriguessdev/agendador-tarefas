spring:
  application:
    name: agendador-tarefas

  datasource:
#    url: jdbc:postgresql://localhost:5432/db_agendador
#    username:
#    password:
    url: jdbc:postgresql://postgres:5432/db_agendador
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081

usuario:
  url: ${USUARIO_URL:http://localhost:8080}